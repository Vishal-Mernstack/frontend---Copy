{
  "name": "hms-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd hms/backend && npm run dev",
    "dev:frontend": "cd hms/frontend && npm run dev -- --host 127.0.0.1 --port 8080"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "csv-parser": "^3.2.0",
    "xlsx": "^0.18.5"
  }
}
